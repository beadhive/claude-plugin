# claude-plugin — repo conventions

- **Before committing or releasing**, read the `commit-conventions` skill
  (`.claude/skills/commit-conventions/`) — commit types drive the version bump here.
- `just check` is the pre-commit gate; `just --list` shows everything else.
